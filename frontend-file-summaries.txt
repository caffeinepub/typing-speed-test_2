{
  "frontend/src/index.css": "ZType gaming theme CSS with all falling-word styles including .falling-word, .word-locked neon glow, .word-explode burst animation, .score-popup-float, .game-input-bar, .neon-input-field, .screen-shake keyframes, .life-loss-vignette, .danger-line, and all existing HUD/combo/multiplier styles preserved.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Orbitron arcade font, Share Tech Mono, neon glow box-shadow utilities, sharp border radius tokens, and animation keyframes for the ZType gaming theme.",
  "frontend/src/App.tsx": "Complete ZType falling-words arcade game with difficulty preset selection (Slow/Medium/Fast) shown on the idle start screen and game-over results; the chosen preset's base speed and spawn interval are applied at game start with automatic per-level scaling on top, and the active preset is displayed as a HUD panel during play. All component prop interfaces are correctly matched (FallingWord uses isTargeted, GameInputBar uses typedText + ChangeEvent handler, FloatingScorePopup uses points).",
  "frontend/src/types/game.ts": "TypeScript types for the falling-words game; restores the original FallingWord shape (typedIndex, status) and ScorePopup shape (points) that existing components depend on, while adding the new DifficultyPreset type, DifficultyConfig interface, and DIFFICULTY_PRESETS constant for slow/medium/fast baseline speeds.",
  "frontend/src/data/wordLists.ts": "Tiered word lists (easy 3-5 letters, medium 6-8 letters, hard 9+ letters) with a getWordForLevel helper that selects words from appropriate pools based on current difficulty level.",
  "frontend/src/components/FallingWord.tsx": "Absolutely-positioned React component rendering a single falling word with progressive character highlighting (matched prefix in neon cyan, remaining in dim), lock-on glow border, and destruction/missed animation classes.",
  "frontend/src/components/FloatingScorePopup.tsx": "Absolutely-positioned floating score popup component that renders a \"+N\" label at a given (x, y) position with an upward float and fade-out CSS animation.",
  "frontend/src/components/GameInputBar.tsx": "Neon-styled fixed input bar component showing current target word hint, live match highlighting, error flash state, and a styled text input with neon cursor. Fixed: updated `inputRef` prop type to `React.RefObject<HTMLInputElement | null>` to match React 19's `useRef` return type (TS2322).",
  "frontend/src/components/DifficultySelector.tsx": "New neon-styled component rendering three toggle buttons (Slow, Medium, Fast) for difficulty selection; active button glows in a preset-specific color (green/cyan/orange) with a pulsing indicator dot, inactive buttons are dimmed."
}